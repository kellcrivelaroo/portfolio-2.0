const Logo = () => {
  return (
    <span className="group block select-none font-alt text-4xl font-bold transition-colors duration-500">
      {`< Kell`}
      <span className="ml-1 text-sky-500">.</span>
      {` />`}
    </span>
  )
}

export default Logo
